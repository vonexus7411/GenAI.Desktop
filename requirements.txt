# GenAI Desktop - Python Requirements
# Compatible with Python 3.10+ (tested with 3.12 and 3.13)
# Optimized for cross-platform compatibility including macOS ARM64

# GUI Framework - Using flexible versioning for better compatibility
PyQt5>=5.15.0,<5.16.0

# HTTP Client Libraries
requests>=2.25.0
aiohttp>=3.8.0

# Data Processing - Flexible versions for ARM64 compatibility
numpy>=1.21.0
PyYAML>=6.0
python-dateutil>=2.8.0

# HTTP Dependencies - Let pip resolve compatible versions
certifi
charset-normalizer
idna
urllib3>=1.26.0

# Build Tools
setuptools>=60.0.0
wheel

# Additional Dependencies
asyncio-mqtt>=0.13.0
websockets>=10.0
markdown>=3.3.0
pygments>=2.10.0

# Development Dependencies (optional)
# Uncomment for development
# pytest>=7.0.0
# black>=22.0.0
# flake8>=5.0.0
# mypy>=1.0.0

# macOS ARM64 specific notes:
# - If PyQt5 installation fails, try: pip install --only-binary=all PyQt5
# - For Apple Silicon, ensure you're using native Python, not Rosetta
# - Alternative: Use conda-forge channel for better ARM64 support
