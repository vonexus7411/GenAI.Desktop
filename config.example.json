{
  "api": {
    "host": "localhost",
    "port": 1234,
    "base_url": "http://localhost:1234",
    "timeout": 30,
    "max_retries": 3
  },
  "ui": {
    "theme": "default",
    "font_family": "Arial",
    "font_size": 10,
    "window_width": 1200,
    "window_height": 800,
    "auto_scroll": true,
    "save_window_state": true,
    "show_timestamps": true,
    "word_wrap": true
  },
  "generation": {
    "default_temperature": 0.7,
    "default_max_tokens": 1000,
    "default_top_p": 0.9,
    "default_frequency_penalty": 0.0,
    "default_presence_penalty": 0.0,
    "enable_streaming": true,
    "stream_chunk_size": 1024
  },
  "version": "1.0.0",
  "_comment": {
    "description": "GenAI Desktop Configuration File",
    "api_section": "Configuration for LM Studio API connection",
    "ui_section": "User interface settings and appearance",
    "generation_section": "Default parameters for text generation",
    "notes": [
      "This is an example configuration file.",
      "Copy this file to config.json and modify as needed.",
      "The application will create a default config if none exists.",
      "All settings can be modified through the Settings dialog."
    ],
    "api_settings": {
      "host": "LM Studio server hostname (usually localhost)",
      "port": "LM Studio server port (default 1234)",
      "base_url": "Full URL to LM Studio server (auto-generated if empty)",
      "timeout": "Request timeout in seconds",
      "max_retries": "Maximum retry attempts for failed requests"
    },
    "ui_settings": {
      "theme": "UI theme: 'default', 'dark', or 'light'",
      "font_family": "Font family for the interface",
      "font_size": "Font size in points",
      "window_width": "Default window width in pixels",
      "window_height": "Default window height in pixels",
      "auto_scroll": "Automatically scroll to new messages",
      "save_window_state": "Remember window size and position",
      "show_timestamps": "Show timestamps on messages",
      "word_wrap": "Enable word wrapping in chat display"
    },
    "generation_settings": {
      "default_temperature": "Controls randomness (0.0-1.0, higher = more random)",
      "default_max_tokens": "Maximum tokens to generate per response",
      "default_top_p": "Nucleus sampling parameter (0.0-1.0)",
      "default_frequency_penalty": "Reduce repetition (-2.0 to 2.0)",
      "default_presence_penalty": "Encourage topic diversity (-2.0 to 2.0)",
      "enable_streaming": "Enable real-time streaming responses",
      "stream_chunk_size": "Size of streaming chunks in bytes"
    }
  }
}
